# vla_inference_accelerator

FPGA accelerator for the OpenVLA-OFT **`L1RegressionActionHead`** (`MLPResNet`): offloads action-token hidden states → robot action chunk inference over PCIe.

See `docs/OpenVLA-OFT.md` for architecture and `docs/Pipeline.md` for development phases.
